{% extends 'base.html.twig' %}
{% block content %}
 <div class="container-fluid">
        <div class="row">
          <div class="col-12">
           
            <!-- /.card -->

            <div class="card">
              <div class="card-header d-flex align-items-center">
                <h3 class="card-title">Liste des categories</h3>
              </div>
              <!-- /.card-header -->
              <div class="card-body">
              <table id="example1" class="table table-bordered table-striped">
    <thead>
      <tr>
       
     
        <th>Nom </th>
        <th>Code Raccourci</th>
         <th>Actions</th>
       
      </tr>
    </thead>
    <tbody>
      {% for categorie in categories %}
                <tr>
                    <td>{{ categorie.getNom() }}</td>
                    <td>{{ categorie.getCodeRaccourci() }}</td>
                 <td>
                  <ul>
                                {% if entite=='licencie' %}
                                    <a class="nav-link"  href="{{ path('app_liste_licencie_categorie', { 'id': categorie.id }) }}"><span class="far fa-file-alt"></span></a>
                               {% else %}
                                    <a class="nav-link"  href="{{ path('app_liste_contacts_categorie', { 'id': categorie.id }) }}"><span class="far fa-file-alt"></span></a>
                               
                              {% endif %}
                                          </ul>      
                 </td>
                  
                  
                  
            {% endfor %}
    </tbody>

  </table>
                
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
{% endblock %}